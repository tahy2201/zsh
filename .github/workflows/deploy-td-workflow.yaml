name: Container
on: push

jobs:
  deploy-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: deploy
        run: |
          git diff origin/${GITHUB_BASE_REF}...origin/${GITHUB_HEAD_REF}

